# 📋 糖尿病患者再入院预测项目 —— 任务看板 (todo.md)

以下是当前项目的进度追踪：

- [x] **任务 1：初步数据探索**
  - [x] 加载当前目录下的 `diabetic_data.csv`。
  - [x] 在 Notebook 中输出前 5 行、总行数和总列数。
  - [x] 大白话解释 `time_in_hospital`、`num_lab_procedures` 和 `readmitted` 的医学含义。

- [x] **任务 2：专业特征工程**
  - [x] 剔除已死亡或进入临终关怀的患者记录（数据清洗）。
  - [x] 过滤重复就诊，每个患者只保留第一次就诊记录（防止数据泄漏）。
  - [x] 删除缺失率极高及非临床特征（`weight`，`payer_code`）。
  - [x] 将年龄区间转化为中位数数值，并将 ICD-9 诊断代码分类映射为 9 大类。
  - [x] 对类别特征进行独热编码 (One-Hot Encoding)。

- [x] **任务 3：模型训练与对比**
  - [x] 划分 80% 训练集与 20% 测试集。
  - [x] 调整类别权重，训练**随机森林**和**XGBoost**模型以应对数据不平衡。
  - [x] 计算两个模型的 Accuracy, ROC AUC 并在测试集上测试。

- [x] **任务 4：模型深化与可解释性 (SHAP)**
  - [x] 计算并使用 `seaborn` 绘制混淆矩阵（Confusion Matrix）热力图。
  - [x] 打印 Recall（召回率）和 F1-Score。
  - [x] 使用 **SHAP 归因解释** 对 XGBoost 黑盒模型进行可视化（避开兼容性 Bug 使用黑盒解释器），并配以大白话医学解释。

- [x] **任务 5：模型保存与网页端开发 (Deployment)**
  - [x] 将最佳模型（XGBoost）及特征组件保存为本地 `best_diabetes_model.pkl` 文件。
  - [x] 新建 `app.py` 网页应用代码，利用 Streamlit 框架设计包含 5 个最核心特征的风险评估系统。
  - [x] 设计“低风险 / 中风险 / 高风险”三色预警卡片与临床干预建议。
  - [x] 建立并动态勾选 `todo.md` 看板。

- [x] **任务 6：网页端预测异常排查与概率校准 (Bug Fix)**
  - [x] 比对 `app.py` 的特征输入与训练模型时最终特征列名称和顺序，确认输入特征对齐。
  - [x] 诊断模型预测概率恒为“高风险”的原因，发现原因为 XGBoost 训练时使用 `scale_pos_weight` 导致预测概率未校准（整体大幅偏高）。
  - [x] 在 `app.py` 中引入数学校准公式，还原真实世界糖尿病患者再入院发生率。
  - [x] 成功修复“恒为高风险”的 Bug，实现多维度指标输入的动态风险三色卡预警切换。
